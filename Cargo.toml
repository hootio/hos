[package]
name = "hos"
version = "0.1.0"
edition = "2021"
authors = ["Hootan Shadmehr <hootan@umich.edu>"]

[profile.dev]
panic = "abort" # disable stack unwinding on panic

[profile.release]
panic = "abort" # disable stack unwinding on panic

[dependencies]
